// Committed into every ExecutionCertificate (vmConfigHash's sibling): a
// replay is only meaningful against the kernel version that produced it.
export const KERNEL_VERSION = "causal-kernel@0.1.0";
